name = "dh-workplace-worker"
main = "cloudflare-worker.js"
compatibility_date = "2024-01-01"

# Uncomment and configure when ready for production
# [env.production]
# vars = { ENVIRONMENT = "production" }
